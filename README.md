# screenshot-basic

**Standalone dependency** for the [RX Framework](https://docs.rxcore.org) server.

## Description

Screenshot utility for server tools.

## RX Framework usage

Started via `ensure [standalone]` in `server.cfg`. Required by one or more `rx-*` resources.


## License

Third-party components retain their original licenses. This resource is bundled with the RX Framework server.
